import { loadFont } from "@remotion/google-fonts/WorkSans";
export const { fontFamily } = loadFont();

export const { semibold, bold } = {
  semibold: 500,
  bold: 700,
}

export const { primary, black, white, dark } = {
  primary: "#40D183",
  black: "#121212",
  white: "#EEEEEE",
  dark: "#595959",
}